"""Kosh's FastAPI layer - see api/main.py for the app and routes.

Built after the code freeze as an explicit, dated scope extension (the
brief's own optional "FastAPI for the API layer" stretch goal). See
RESULTS.md's "Post-submission stretch goals" section for the full story of
what this is and why it's separate from the frozen engine below it.
"""
